name: easix
description: easix is a helper package that provide multiple handy function and extensions to use in your flutter project.
version: 2.0.0
homepage: https://github.com/Mustafa7Ibrahim/easix.git

environment:
  sdk: ">=3.1.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  cached_network_image: ^3.4.1
  flutter_svg: ^2.0.17
  intl: ^0.20.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2
